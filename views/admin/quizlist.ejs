<%-include("header.ejs")%>
<div class="container pt-5">
    <div class="row align-item-center justify-content-center">
        <h2>Quiz List</h2>
        <%for(let i=0;i<quizes.length;i++){%>
            
            <div class="col-md-10 rounded shadow p- mb-3">
   <div class="row p-3">
    <div class="col-1 text-center">
        <h4><%-i+1%></h4>
    </div>
    <div class="col-8">
      <h4><%-quizes[i].quiz_name%></h4>  
       <small><%-quizes[i].quiz_time%> hr, <%-quizes[i].total_mark%> marks</small>
    </div>
<div class="col-3">
    <a class="btn btn-primary" href="/admin/responses/<%-quizes[i]._id%>">Responses</a>
    <a class="btn btn-success" href="/admin/attempt-quiz/<%-quizes[i]._id%>">Attempt</a>
    <button class="btn btn-danger">X</button>
</div>
</div> 

</div>
            <%}%>

    </div>
</div>
<%-include("footer.ejs")%>
